export default function robots() {
  return {
    rules: {
      userAgent: '*',
      //allow: '/',
      disallow: '/',
    },
    //sitemap: 'https://next-portfolio-phi-black.vercel.app/sitemap.xml',
  }
}